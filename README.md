# React + Redux Scoreboard
A scoreboard with a stop watch built with React + Redux

## Project set up
Install dependencies with `npm install`

## Running the app
Run `npm start` to serve the app with WebPack
